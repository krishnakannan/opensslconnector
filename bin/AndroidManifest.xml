<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gsoc.ssl_connector"
    android:versionCode="2"
    android:versionName="1.1" >

    <uses-sdk
        android:minSdkVersion="11"
        android:targetSdkVersion="21" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MessageViewWindow"
            android:label="@string/title_activity_message_view_window" >
        </activity>
        android:name="com.gsoc.ssl_connector.LoadCipherSuites"
        <activity
            android:name=".ChooseCert"
            android:label="@string/title_activity_choose_cert" >
        </activity>
        <activity
            android:name=".CipherSuites"
            android:label="@string/title_activity_cipher_suites" >
        </activity>
    </application>

</manifest>
